import actions from "./action";

const initState = {
  createAssessmentLoading: false
};

export default function assessmentReducer(state = initState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
